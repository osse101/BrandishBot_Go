{
  "name": "brandishbot-go-root",
  "private": true,
  "devDependencies": {
    "prettier": "^3.4.2"
  },
  "scripts": {
    "format": "prettier --write . | grep -v '(unchanged)' || true",
    "format:check": "prettier --check . | grep -v '(unchanged)' || true"
  }
}
